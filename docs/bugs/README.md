# Known Bugs & Resolved Issues

This directory documents significant bugs encountered during the port, their symptoms, root causes, and fixes. When fixing a new bug, add an entry here so future sessions can recognize the same class of issue.

## Index

| Date       | Slug                                                                                                                         | Summary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-02 | [netplay_lbparticle_free_list_cycle_watchdog](netplay_lbparticle_free_list_cycle_watchdog_2026-07-02.md)                     | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — after removing the former `transient_effect_probe` skip, a Fox Firefox collision soak exposed a Linux watchdog with frozen `frame`/`yield_count` and repeated backtraces in `lbParticleTransformIsOnFreeList` / `lbParticleTransformIsAllocated`. Root cause: netmenu `xf` liveness validation walked LBParticle free/live lists without bounds, so rollback verify/effect-enforce churn could expose a double-pushed `LBTransform` free-list cycle and spin forever. Fix: side-track transform free/live state, reject double-free pushes before `proc_dead` re-entry, and bound/log netmenu list validation walks instead of hanging. |
| 2026-07-02 | [netplay_fox_firefox_resim_load_catchup](netplay_fox_firefox_resim_load_catchup_2026-07-02.md)                               | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — soak2 `1837159318`/`824143646`: `FORCE_MISMATCH` @520 during Fox air Firefox; Android `PEER_SNAPSHOT_DIVERGE` @518 while Linux resim OK. Root cause: Fox/Pikachu `LaunchIfDue`/`EndIfDue` on resim anchor loads and pre-baseline apply (sim tick ahead of load_tick). Fix: `syNetRollbackResimGateCatchUpAllowed()` gates status-advancing catch-up on `BeginResimInitialLoad` + baseline gate (no new probe skips). | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-01 | [netplay_resim_replay_hang_firefox_charge](netplay_resim_replay_hang_firefox_charge_2026-07-01.md) | **DIAGNOSTIC ADDED (`PORT && SSB64_NETMENU`)**, not yet fixed — soak2 session `152487374` with `FORCE_MISMATCH` @520: both peers hard-lock during resim replay at Fox air-Firefox charge (`SpecialAirHiHold`), watchdog stall in `gcRunGObjProcess` inside `scVSBattleFuncUpdateBattleSimOnly` called from packet ingress; NetSync paired through 520 (not drift). Suspect synchronous resim replay inside ingress + interface thread recv on empty `gGCMesgQueue`. Added `SSB64_NETPLAY_RESIM_REPLAY_HANG_DIAG` (+ optional `_VERBOSE`) with ingress depth, replay gate/tick, `BattleSimOnly` nesting, thread recv wait, watchdog hang snapshot, Fox Firefox context. |
| 2026-07-01 | [netplay_kirby_shieldpose_figatree_token_corruption](netplay_kirby_shieldpose_figatree_token_corruption_2026-07-01.md) | **FIX IMPLEMENTED (all builds, not netplay-only)**, soak pending — soak2: recurring `RelocPointerTable: invalid/stale token` from `lbcommon.c:894` while shielding, plus Kirby's model joints spinning/permanently corrupting after he shields. Traced Kirby's figatree-bind trail: a Guard/GuardOff joint table stable at `bound=18` for 7 binds permanently dropped to `bound=16` right at a rollback restore pass — 2 joints silently and permanently lost their animation binding (persistent intern-buffer data, corruption sticks for the rest of the process). Root cause: `portRelocIsFighterFigatreeFile()` only recognizes `reloc_animations/FT*`/`reloc_submotions/FT*`/`SCExplainMain`, missing every character's `*ShieldPose` file (`reloc_fighters_main/KirbyShieldPose` etc.) even though `ftMainRefreshFigatreeVisual` hands ShieldPose data to `lbCommonAddFighterPartsFigatree` identically to a real figatree file — so ShieldPose loads never got the figatree halfswap fixup or `AObjEvent32` byte-order range registration, leaving some non-chain joint words misread as bogus (`gen=0`, out-of-range) tokens. Fix: classify `*ShieldPose` files as figatree files (suffix match, since `reloc_fighters_main/` also holds non-figatree `*Main`/`*MainMotion`/`*Special1-4` tables). Also added `table_ptr`/`table_digest`/`motion`/`status` fields to the existing figatree-bind diagnostic for future bisection. Affects every character's Guard/GuardOff shieldpose motions, not just Kirby. Built clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-01 | [netplay_guard_shield_sentinel_relink](netplay_guard_shield_sentinel_relink_2026-07-01.md) | **FIX IMPLEMENTED**, soak pending — dual-shield-spam soak2 froze both peers with `WATCHDOG HANG` (`frame`/`yield_count` never advance) escalating to `SIGABRT` in `efManagerShieldProcUpdate`/`gcRunGObjProcess`/`gcRunAll`, right as a fresh shield-bubble `GObj` was allocated at the churned pool id `1011`. Root cause: `syNetRbSnapUnlinkSentinelGObjFromLists` unconditionally re-unlinked already-fully-ejected (`obj_kind==0xFE`) gobjs using stale/repurposed `link_prev`/`link_next` (the latter is the `sGCCommonHead` free-list chain pointer post-eject), splicing the free list into the live common-link chain and pinning `gcRunAll`'s walk. Fix: verify real list membership before unlinking; skip entirely when the gobj was already removed by the original `gcEjectGObj`. |
| 2026-07-01 | [netplay_fox_firefox_start_resim_drift](netplay_fox_firefox_start_resim_drift_2026-07-01.md)                                 | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — soak2 `364064941`: forward sim agreed on Fox `SpecialHiStart` (230) @515–520; resim after `FORCE_MISMATCH` @520 passed load verify @518 then immediately `LOAD_SLOT_LIVE_DRIFT` + `PEER_SNAPSHOT_DIVERGE` (Android host) as live jumped to Hold/Travel while ring still had Start. Root cause: gate sanitize/catch-up ran on Start scope, stomping the wrong `status_vars` overlay and advancing the state machine at load/verify time. Fix: `syNetplayFoxFighterInFirefoxStartScope` + scope-aware sanitize + skip catch-up on Start in load-verify and forward-resim presentation paths.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-01 | [netplay_guard_shield_yrotn_draw_matrix](netplay_guard_shield_yrotn_draw_matrix_2026-07-01.md) | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — all-maps shield/fighter ground-clip during guard (synctest on/off): sim/cargo correct but draw used stale `joints[YRotN]->FTParts.mtx_translate`. Animlock-safe `syNetRbSnapRefreshGuardShieldYRotNDrawMatrix` before shield draw; `RebuildIntroFighterPartTransforms` no longer calls blanket transform invalidation. Supersedes attach-refresh / part-transform-attach approaches. |
| 2026-07-01 | [netplay_guard_shield_attach_refresh_diag](netplay_guard_shield_attach_refresh_diag_2026-07-01.md)                           | **REGRESSION ROOT-CAUSED + REVERTED (`PORT && SSB64_NETMENU`)** — `syNetRbSnapRefreshGuardShieldJointAttachFromFighters` (commit `9b06ece`) caused all-maps animlock stomp via blanket `transform_update_mode` invalidation; attach re-pin was a no-op. Reverted. Presentation fix: [netplay_guard_shield_yrotn_draw_matrix](netplay_guard_shield_yrotn_draw_matrix_2026-07-01.md). Sector Z mid-fighter depth still open separately. |
| 2026-07-01 | [netplay_fox_firefox_travel_truncation_diag](netplay_fox_firefox_travel_truncation_diag_2026-07-01.md)                       | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — Firefox resoak with `FOX_FIREFOX_ANIM_PROBE` showed `capture_raw` preserving positive `fox.specialhi.anim_frames`, then `capture_final` zeroing it before save. Root cause: `syNetRbSnapScrubInactiveStatusVarsInBlob` scrubbed inactive `common.dead` / `common.rebirth` overlays while Fox Firefox owned `status_vars.fox.specialhi`; those union aliases overlap `anim_frames` at offset 12, so later rollback apply restored `0` and the load-verify gate ended Travel early. Fix: skip inactive common-overlay scrub for Fox Firefox Start/Hold/Travel/End/Bound, matching the existing Kirby Stone ownership guard. |
| 2026-07-01 | [netplay_kirby_vulcanjab_efstruct_null](netplay_kirby_vulcanjab_efstruct_null_2026-07-01.md)                                 | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`)** — soak2 `1339981196`: deterministic `SIGSEGV` on both peers, Linux backtrace symbolized to `efManagerKirbyVulcanJabProcUpdate+0x0` (`x0=0x0`, `fault_addr=0xe0`) — reproduced with Kirby A-spam (rapid jab) vs a shielding Fox, crashing the same tick Fox's shield-hit reconciliation runs. Root cause: no NULL guard on `efGetStruct`/`DObjGetStruct` before deref, same defect family as the earlier `efManagerImpactWaveProcUpdate` fix (round 1 of `netplay_fox_appear_firefox_charge_soak2`) — a per-hit VFX GObj can outlive the generic rollback effect-prune pass. Fixed + swept the 6 sibling generic damage-particle proc-update callbacks with the identical unguarded shape (`DamageFlyOrbs`/`DamageSpawnOrbs`/`StarRodSpark`/`DamageFlySparks`/`DamageSpawnSparks`/`DamageSpawnMDust`); vanilla/offline body preserved unchanged in each `#else` branch. Re-gated all 8 guards from bare `#ifdef PORT` to `PORT && SSB64_NETMENU` (the hazard is rollback-only, can't occur offline) as part of the shield-crash follow-up below. Built clean, both `build-netmenu`/`build-offline` link.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-01 | [netplay_shield_effect_efstruct_null](netplay_shield_effect_efstruct_null_2026-07-01.md)                                     | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`)** — soak2 `604955560`: same NULL-`EFStruct` SIGSEGV family, this time in `efManagerShieldProcUpdate+0x0` (`fault_addr=0xe0`), reproduced by spamming Guard rapidly on both fighters. Root cause: guard-bubble `GObj` freed via `syNetRbSnapEjectGObj`/`efManagerNetSafeFreeStruct` (`guard_shield_prune path=eject reason=is_release`) while its proc-update callback still runs once more that tick. Fixed `efManagerShieldProcUpdate` (guard+eject) and swept the two sibling display callbacks with the identical unguarded shape, `efManagerShieldProcDisplay` and `efManagerYoshiShieldProcDisplay` (the latter also derefs `ftGetStruct(ep->fighter_gobj)` unchecked); display callbacks skip drawing instead of ejecting. Also used this pass to re-audit and tighten the sibling `netplay_kirby_vulcanjab_efstruct_null` guards to `PORT && SSB64_NETMENU` per the OFFLINE vs NETMENU contract. Both `build-netmenu`/`build-offline` rebuild and link clean. Does not fix the separate shield-clipping visual bug (see `netplay_guard_shield_attach_refresh_diag`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-01 | [netplay_yoshi_egg_lay_synctest_unskip](netplay_yoshi_egg_lay_synctest_unskip_2026-07-01.md) | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`)** — removed blanket `yoshi_egg_lay`/`yoshi_egg_lay_attack` synctest skips and `yoshi_egg_lay_probe` fragile walkback; expanded presentation refresh + effect reconcile to full egg-lay window (victim + attacker) on load-verify/anchor-resync/forward-resim. Built clean; soak pending. |
| 2026-07-01 | [netplay_yoshi_egg_lay_attack_scope_fkind](netplay_yoshi_egg_lay_attack_scope_fkind_2026-07-01.md)                           | **FIX IMPLEMENTED (`PORT`)** — fkind guard on `syNetRbSnapStatusInYoshiEggLayAttackScope` stopped Fox Firefox charge (status 232) from mis-firing `yoshi_egg_lay_attack` skip. Skip removed entirely by synctest un-skip above. |
| 2026-07-01 | [netplay_fox_appear_firefox_charge_soak2](netplay_fox_appear_firefox_charge_soak2_2026-07-01.md)                             | ✅ **RESOLVED, round 6** — soak2 session `2047760919`: FC @600 `eff+figh` during Firefox charge (rng OK); Appear @523 joint fork; Linux SIGSEGV in charge ImpactWave during FC recovery. Round 1: Appear/SpecialN/Firefox per-tick re-pin, joint quantize, ImpactWave reconcile/eject, effect-repair scope, catch-up gate. Round 2 (session `1691872615`): crash/FC fixed, but `fox_firefox_probe` fragile-anchor gate walked resim load back through the whole Firefox window (519→487); removed the gate. Round 3 (session `453641924`): walkback fixed, but blanket `SYNCTEST_SKIP reason=fox_firefox` in `syNetRbSnapshotSynctestShouldSkip` was hiding a real joint-fold divergence (first split @523, Fox ground `SpecialHi`); removed that skip too so synctest can localize the fold gap instead of masking it. Round 4 (session `2047424634`): `SYNCTEST_FAIL` never fired (`RESULT: PASS`) yet host hit `PEER_SNAPSHOT_DIVERGE` @518 and stopped the VS session — traced to `effect_count_transition_probe` (Fox charge ImpactWave spawn/despawn) being the one fragile-probe branch **not** gated by `s_syNetRbSnapResimAnchorEffectRepairTolerant`, so it kept walking the resim load anchor back (518→517→516→515) into a tick whose forward resim produced Fox's Hold→Travel transition one tick earlier than the original forward-sim pass had (same-peer resim-vs-original self-inconsistency, confirmed via duplicate `fighter_slot_hash`/`map_hash_save` lines for the same ticks showing status `232` vs `234`). Gated it behind the same tolerant flag as its siblings. Round 5 (session `2061522853`): round 4 held, but "first split @523" recurred at the _same fixed tick_ on every one of 5 soaks regardless of gameplay content — traced to the diagnostic trace itself sampling fighter state **before** the per-tick quantize pass (see `netplay_sim_state_trace_pre_quantize_diag`); real remaining divergence is `fhash_light` forking during Fox `SpecialHiHold` (status 232) on the Sector Z Arwing deck, not yet isolated to a field — needs a fresh soak with the corrected trace. Round 6 (session `1339981196`, longest soak yet, `max_sim_tick=1750`): round 5 held with zero Firefox-related `FRAME_COMMIT`/`SYNCTEST` failures — closing this thread. Two unrelated issues surfaced in the same soak and are tracked separately: `netplay_kirby_vulcanjab_efstruct_null` (SIGSEGV) and `netplay_yoshi_egg_lay_attack_scope_fkind` (bogus skip reason on Firefox charge, coincidental status-ID collision).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-01 | [netplay_sim_state_trace_pre_quantize_diag](netplay_sim_state_trace_pre_quantize_diag_2026-07-01.md)                         | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`)** — `fighter_slot_hash`/`sim_state_tick` diagnostic trace was sampled inside `syNetPeerUpdate()`, **before** `syNetRollbackAfterBattleUpdate()`'s quantize pass, so it logged raw pre-quantize state while the real frame-commit token compares the post-quantize ring value. Caused every Fox-Firefox soak to report a spurious "first mismatch" at the same fixed lag-boundary tick regardless of gameplay content (a self-correcting rollback misprediction, not a real bug). Moved the trace call to `scVSBattleFuncUpdate()` right after `syNetRollbackAfterBattleUpdate()`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-01 | [netplay_fox_firefox_resim_cross_isa](netplay_fox_firefox_resim_cross_isa_2026-07-01.md)                                     | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — soak2 @600 `figh,rng` FC diverge with Fox `SpecialAirHi` travel. Gate timer sanitize + per-tick angle/physics canonicalize + forward resim slot re-pin + post load-verify catch-up.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-01 | [netplay_load_hash_camera_roundtrip](netplay_load_hash_camera_roundtrip_2026-07-01.md)                                       | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — Cam-only `LOAD_HASH_DRIFT` on both peers @389/480/519/…: `ApplyCamera` integrate mutates hashed GMCamera fields during load verify. Skip integrate when load-hash verify enabled; integrate on forward resim ticks instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-01 | [netplay_sector_z_patrol_map_hash_verify](netplay_sector_z_patrol_map_hash_verify_2026-07-01.md)                             | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — Post patrol un-skip soak: `SYNCTEST_FAIL` @1100+ with map-only drift (`map_hash_yaku1` ~103 u gap, `verify_skip_all`). Root cause: verify-only skipped `ApplyArwing` while map hash reconciled deck from unrestored live flight tree. Fix: restore slot Arwing partition at hash boundary via `ComputeMapHashLiveForVerify`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-01 | [netplay_guard_shield_part_transform_attach](netplay_guard_shield_part_transform_attach_2026-07-01.md)                       | **SUPERSEDED** — attach-refresh after rebuild reintroduced animlock stomp; see [netplay_guard_shield_yrotn_draw_matrix](netplay_guard_shield_yrotn_draw_matrix_2026-07-01.md). |
| 2026-07-01 | [netplay_resim_complete_guard_shield_attach](netplay_resim_complete_guard_shield_attach_2026-07-01.md)                       | **SUPERSEDED** — see [netplay_guard_shield_yrotn_draw_matrix](netplay_guard_shield_yrotn_draw_matrix_2026-07-01.md). |
| 2026-07-01 | [netplay_sector_z_arwing_patrol_synctest_unskip](netplay_sector_z_arwing_patrol_synctest_unskip_2026-07-01.md)               | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — Re-enabled synctest during Sector Z Arwing patrol by removing `sector_arwing_patrol` / `sector_arwing_patrol_probe` skip gates (~209 skipped ticks in `soak2` hid save→restore oracle). Deck-coupled skip retained. Expect possible `SYNCTEST_FAIL` / map-hash drift during patrol until N−1 probe snap is fixed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-01 | [netplay_gameplay_resim_camera_cobj_freeze](netplay_gameplay_resim_camera_cobj_freeze_2026-07-01.md)                         | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — Sector Z FC resim (load @360 → @481): fighters OK after deck-carry fix but **battle camera frozen** (viewport stuck at intro load-tick CObj while fighters move on deck). Root cause: `cobj_valid` restore pins eye/at but integrate only ran during intro Wait; `syNetSyncHashGMCamera` excludes CObj so hash can move with a frozen view. Fix: integrate after apply on any rollback load; CObj restore from target_tick + integrate at gameplay resim complete; resync path same gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-01 | [netplay_sector_z_resim_fox_deck_stale_translate](netplay_sector_z_resim_fox_deck_stale_translate_2026-07-01.md)             | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — Sector Z cross-ISA resim (`soak2`, load 360→481): Fox on Great Fox deck (`floor_line=1`) **teleports ~769 u** during grounded SpecialN after rollback; Pikachu unaffected; both peers identical (deterministic replay hole). Root cause: deck-derived `mp_yaku[1]` skip on apply leaves yakumono translate at pre-load live position while flight tree restores to load tick → `UpdateCollisions` derives bogus `gMPCollisionSpeeds[1]` → `ftmain` platform carry slides grounded fighters. Fix: in `grSectorArwingReconcileDeckYakumonoFromFlightTree`, snap stale translate (gap > ~45 u) with zero speed before per-frame SetPos when rollback semantics active.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-06-30 | [netplay_resim_anchor_probe_postload_resync](netplay_resim_anchor_probe_postload_resync_2026-06-30.md)                       | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — `soak2` DK throw resim @503: anchor probe compared ring `@load` after `ResyncLiveFightersFromSlotForSim` (joint repair for +1 sim) → spurious `postload_f=1 step_f=0` on every tick → 16-step walkback; Android follower anchored @487 vs Linux @503, off-by-one resim state. Fix: postload oracle before resync; `POSTLOAD_BYPASS` when load is one tick ahead but +1 probe matches ring. Built clean (`build`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-06-30 | [netplay_synctest_quake_orphan_duplicate](netplay_synctest_quake_orphan_duplicate_2026-06-30.md)                             | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — (509) synctest eff-only fail from duplicate bomb quakes (`count=2` vs slot `1`); (3595) verify empty fold; **(3404)** two identical `pri=2` quakes same ring `gobj_id` after enforce — blob-matching id-collision duplicate skipped enforce eject. Fix: shell/unmatched prune + canonical-ptr guard + surplus blob-matched quake dedupe + enforce eject all non-canonical reconciled-id ptrs. Built clean (`build-netmenu`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-06-30 | [netplay_rebirth_halo_ef_pool_desync](netplay_rebirth_halo_ef_pool_desync_2026-06-30.md)                                     | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — `soak2` session `11770459` (synctest STABLE, quake clean) but Kirby **second-stock** rebirth @tick 2532 missing platform while Link still in `RebirthDown` (dual rebirth). `make_effect_fail step=ef_struct free=26` + `halo_ensure_fail reclaimed=0` on both peers = `sEFManagerStructsAllocFree==NULL` while `free_num==26` (counter/list desync, not exhaustion; same pool family as quake free-list corruption). Fix: `efManagerNetAuditPool`/`RebuildFreeList` + auto-retry in `GetNextStructAlloc`; enhanced `make_effect_fail` logs `list_len`/`alloc_free`/`in_use`/`live_eff_gobjs`; forward `syNetRbSnapForwardPruneStaleRebirthHalosForMint` + audit in reclaim; vanilla `ftCommonRebirthDownSetStatus` calls reclaim before mint. Built clean (`build-netmenu` + `build-offline`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-06-30 | [netplay_link_bomb_effect_desc_sigsegv](netplay_link_bomb_effect_desc_sigsegv_2026-06-30.md)                                 | **GUARD + DIAGNOSTIC SHIPPED (`#ifdef PORT`, soak pending)** — `soak2` cross-ISA pair **crashed deterministically on both peers** running **Link bomb** tests (`sigsegv=1`, no `FRAME_COMMIT_*`/`SYNCTEST_FAIL`, `resim=0` → forward-sim, not a desync). The trimmed log dropped the backtrace and the Android handler is a no-op stub (`backtrace`/`backtrace_symbols_fd` stubbed for `__ANDROID_API__ < 33` in `port_watchdog.cpp`); the **Linux** glibc backtrace in the untrimmed log pins it: `efManagerMakeEffect+0x35`, `fault_addr=0x7fa5ffffffff` / `x5=0xffffffff` (one frame deep — `fp=0x4`, omit-frame-pointer build, so the caller/effect is unshown). Trigger: Link `status=110` LightThrowF4 → bomb explode (`itLinkBombExplodeMakeEffectGotoSetStatus`) fires `efManagerQuakeMakeEffect`/dust through `efManagerMakeEffect` (the sparkle uses `lbParticleMakeScriptID`, not the crash path). Cause class: same family as the existing `*effect_desc->file_head==NULL` Kirby stale-GObj guard but the **non-NULL variant** — a pointer (`effect_desc`, its `file_head`, or the resolved base) whose **low 32 bits were stomped with `0xFFFFFFFF`** = a 32-bit `-1` sentinel written into a 64-bit pointer (benign on N64's 32-bit space, fatal on LP64: `0x7fa5_ffffffff` = real base OR all-ones low word). Fix (decomp `ef/efmanager.c`, `#ifdef PORT` matching the adjacent guard): `syEfDescPtrPlausible` (rejects `<0x1000`, low-word `0xFFFFFFFF`, `-1`) + entry guard on `effect_desc` before the first deref + extended resource guard validating `file_head` before deref and the resolved base after, all logging the caller via `__builtin_return_address(0)`. Converts the crash into a logged, cleanly-skipped effect that **names the caller** for the root-cause follow-up. Built clean (offline `ssb64`). Not caused by the 06-29 collision/coupling fixes (effect creation untouched; `resim=0` so snapshot finalize never ran). Audit hook: cross-ISA SIGSEGV with `fault_addr` low 32 = `0xFFFFFFFF` (+ matching `0xffffffff` register) = a 32-bit `-1` written into a 64-bit pointer; symbolize the Linux peer (Android handler is stubbed <API 33); effect crashes resolve resources via `*effect_desc->file_head` — validate the pointer, not just `==NULL`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-06-29 | [netplay_fighter_child_joint_rotate_quantize](netplay_fighter_child_joint_rotate_quantize_2026-06-29.md)                     | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — Cross-ISA (Android host / Linux guest) Peach's Castle DK-vs-Link: grab with DK + cargo-carry (Link Shouldered) + run → rollback resim during the carry → genuine `FRAME_COMMIT_STATE_DIVERGE [figh]` with **identical inputs** (`fc_div_inputs_match=1`) → `PEER_BASELINE_RESYNC_STORM`/`VS_SESSION_END`. Field drill-down: `status_id`/`motion_id` **match** cross-peer but `fhash_full`/`anim_hash` diverge → it's the **joint pose (rotations)**, not control flow; host-only `SYNCTEST_FAIL`@509 showed **live (quantized `0xBFC91000`) vs blob (raw `0xBFC90FD8`)** ≈ -π/2 flipping a landing-status decision. Root cause: `fhash_full`/FC `fighter_digest` fold every joint's translate **and rotate**, but the two in-sim quantize sites left a hole — (1) `gcPlayDObjAnimJoint`→`syNetplayQuantizeDObjAnimPose` runs **only when a joint's figatree track advances that frame**; (2) `syNetplayCanonicalizeFighterSimState` (per-fighter, every accepted sim boundary, all fighters) quantized **root** rotate but for **child joints only translate, never rotate/scale**; (3) `syNetplayCanonicalizeFighterIntroJointPose` covers child rotate+scale but is **no-op outside intro/appear**. So a joint **held static or posed via the grab-coupling matrix** (DK heavy-throw/cargo pose; the Shouldered captive) kept a **raw cross-ISA rotate** mid-match → folded into `fhash_full`, copied verbatim into the (intentionally raw-captured) snapshot blob → forked the FC digest. The Ness PK-Thunder fix worked around this for Ness only by re-quantizing the blob at capture (reintroduces `full_ok=0` oracle noise, doesn't generalize). Fix: in `syNetplayCanonicalizeFighterSimState` change both child-joint loops from `syNetplayQuantizeDObjTranslate` → `syNetplayQuantizeDObjAnimPose` (translate+rotate+scale, the same fn the per-frame hook + intro pass use) so the **live** pose is grid-aligned for non-animating joints in every scope → captured-raw blob is already on-grid (oracle stays clean, FC digest converges). Idempotent for animating joints; intro pass now redundant for children (harmless). Self-gated `syNetplaySimQuantizeActive()`; offline never compiles the TU. Built clean (`build-netmenu`). Audit hook: cross-ISA `figh`-only `FRAME_COMMIT_STATE_DIVERGE` + identical inputs + matching `status`/`motion` but diverging `fhash_full`/`anim_hash` = a **joint rotate** hashed/folded but only quantized in-sim when its track advances; any DObj field in a rollback hash must be quantized at the sim boundary for _all_ fighters/scopes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-06-29 | [netplay_grab_coupling_skip_anchor_masking](netplay_grab_coupling_skip_anchor_masking_2026-06-29.md)                         | **SKIP REMOVED PERMANENTLY (2026-06-29 update); root-cause hunt in progress** — the grab-coupling synctest skip, probe-skip, release-boundary probe, the `SSB64_NETPLAY_DISABLE_GRAB_COUPLING_SKIP` env switch, and the load-anchor fragility they drove are all deleted, so the failure is always exposed. With the skip gone the DK cargo-carry desync localizes to a **forward-sim vs resim landing-timing fork**: at tick 637 the blob↔live diff is **`status_id`/`motion_id` only** (forward sim left DK stuck in `ThrowFFall` 241 while resim lands him to 242), and at the fork tick 569 **every dumped per-fighter field is bit-identical cross-peer** yet committed `status` forks. The `landing_branch` floor verdict `vv0` is deterministic cross-peer; remaining suspects are the unlogged `mpProcessCheckTestFloorCollisionAdjNew` gate inputs (`floor_flags & MAP_VERTEX_COLL_PASS`, `floor_line_id != ignore_line_id`) at the landing-check instant (probe now logs `fflags`/`ignore`/`mask_unk`/`gated`). Also fixed: the `fighter_cargo_diag2` witness-stomp false positive (read `status_vars.common.throwf` directly, not via the accessor); and hardened an uninitialized `result` UB in `mpProcessUpdateMain` (returns stack garbage cross-ISA when the collision loop is skipped, now inits `FALSE`). Built clean (`build-netmenu` + `build-offline`). HISTORICAL (superseded): originally — `soak2` Castle pair (current build, `FORCE_MISMATCH INJECT_TICK=520`) no longer crashes at Link's grab on DK and reads **`STABLE` (soft recovery, `resim=1`, no `fc_*_div`/`synctest_fail`)**, yet the grab **visibly fails to replay**: `capture=1` appears nowhere post-rollback, the live grab connected (`SYNCTEST_SKIP reason=grab_coupling` ran ticks 510–520 then 523–531) but resim whiffs (DK `status=10 capture=0`, Link `status=166 catch=1`). A landed grab became a whiff with nothing flagged. Root cause of the _masking_ (two mechanisms, one predicate `syNetRbSnapBlobInGrabThrowSynctestFragileScope`): (1) **verification blackout** — `grab_coupling` / `grab_coupling_probe` / `grab_coupling_release_boundary_probe` skip the save→restore→re-hash on every grab tick so an imperfect replay is never checked; (2) **anchor steering** — `syNetRbSnapshotIsLoadAnchorFragile` _reuses_ `ShouldSkipProbeTick`, so `ResolveLoadAnchorAvoidingFragile` walks the resim load anchor backward off grab ticks (`load_tick=487`, pre-catch, spanning the grab) = "it resimmed before Link did the grab move." The skips are a workaround for the residual grab/catch-coupling save→restore _fidelity_ gap (crash fixed 06-27 by resolving couplings per player slot; round-trip not). Shipped `SSB64_NETPLAY_DISABLE_GRAB_COUPLING_SKIP=1` (`syNetRbSnapGrabCouplingSkipDisabled()`) force-disabling all three skip sites → synctest runs on grab ticks **and** anchor may sit inside the grab. Default OFF = byte-identical current behavior; built clean (`build-netmenu`); offline unaffected (never compiles this TU). Next: re-soak with env=1 on both peers — a `SYNCTEST_FAIL`/`fhash` divergence on ticks 510–531 names the unrestored coupling field; a correct replay means the anchor walkback was over-rewinding. Audit hook: a `STABLE` rollback that visibly changes a gameplay outcome on a window the `*_probe`/live grab skip covers = probe-skip masking, not recovery — flip the env to convert the silent drop into a located failure.                               |
| 2026-06-28 | [netplay_castle_bumper_item_resim_diverge](netplay_castle_bumper_item_resim_diverge_2026-06-28.md)                           | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — Peach's Castle (DK vs Link) item-only cross-peer divergence with **no grab move**. The 06-29 soak's `item_fold_floats` instrument named the field: **all five folded bumper extras (`scale.x/y`/`palette_id`/`multi`/`hit_anim_length`) are bit-identical cross-peer; the diverging field is the bumper position** (`pos.x` android `-838.25` vs linux `-833.00`, a ~5.25-unit gap → not the original scale-quantization suspicion). Root cause: `netplay_sim_quantize.c` canonicalized fighter physics/transforms + camera every accepted tick but had **no item path**, so the movable bumper's un-quantized forward-sim position accumulated cross-ISA f32 drift until a late `frame_commit_item_diverge` (commit frontier `last_agreed=600`) forced a `span=120` recovery resim (600→721) that grinds then tears down via `VS_SESSION_END` (perceived as a hard lock). Fix: added `syNetplayCanonicalizeItemSimState`/`syNetplayCanonicalizeActiveItemsForNetplay` (+ static `syNetplayQuantizeItemPhysics`) snapping each live item's `ITPhysics` (`vel_ground`/`vel_air`) and root DObj translate (the exact folded position) to the shared `1/65536` grid each accepted tick, called from `syNetplayCanonicalizeActiveFightersForNetplay` alongside the camera pass — same determinism strategy as fighters. Gated by `syNetplaySimQuantizeActive()` (netmenu + VS/resim only; no-op offline). Built clean (`build-netmenu` + `build-offline`). Audit hook below; `rng` diverge is downstream (`ring_steps=0`). Original detail: `soak2` cross-ISA pair (current post-06-27-coupling-fix build) on Peach's Castle (DK vs Link) desynced **with no grab move**. Committed per-frame digests are bit-identical through the traced-back origin (ticks 468–490); the only divergent committed tick is 601, differing in **`item` only** (`figh`/`rng`/`world`/`anim`/`cam` all reconcile). Detection (`frame_commit_item_diverge` @599) shows a single live item `gobj_id=1013 kind=23` = **`nITKindGBumper` (the Castle Bumper)**; the co-reported `frame_commit_rng_diverge` has `ring_steps=0/count=0` → RNG is **downstream** of the item, not the cause. Audit result: the bumper rollback fold (`syNetSyncFoldGBumperItemExtras`) folds five fields (`multi`, `hit_anim_length`, `scale.x/y`, `palette_id`) and **all five are captured/restored** — the "dropped field" hypothesis is disproven; it's a **value** divergence, prime suspect the derived float `scale = 2.0 - (10-multi)*0.1` straddling the `1/65536` quantization grid cross-ISA. Existing per-field diag (`item_fold_floats`) was Link-bomb-only, so it couldn't name the bumper field. Fix shipped is **diagnostic-only**: added a `nITKindGBumper` branch to `syNetSyncLogItemFieldDiffDiag` emitting raw+quantized bits of `scale.x/y`/`palette_id` + `multi`/`hit_anim_length`/`unk_0x2`/`damage_all_delay`, gated by `SSB64_NETPLAY_ITEM_HASH_FIELD_DIFF=1`. Built clean (`build-netmenu`); offline links unchanged (never compiles `netsync.c`). Next: re-soak Castle with the env on, diff `item_fold_floats` host-vs-guest — first `*_q` mismatch is the field; if scale, drop the derived float from the fold (Sector `map_gobj_flags` precedent). Audit hook: `item`-only committed divergence + everything else reconciling + single `kind=23` item = Castle Bumper; name the field via `item_fold_floats` before assuming a coverage gap; unrelated to grab coupling. |
| 2026-06-28 | [netplay_quake_effect_pool_free](netplay_quake_effect_pool_free_2026-06-28.md)                                               | **GUARD + DIAGNOSTIC SHIPPED (`PORT && SSB64_NETMENU`, soak pending)** — `soak1` cross-ISA pair (android host / linux guest) **crashed deterministically on both peers at the same forward-sim tick** in `efManagerQuakeMakeEffect` while making a camera quake (`gobj_alloc id=1011 link=6`; Linux `lr=0x0`/`fault_addr=0x0`, Android `fault_addr=0x82b72000000000`; no resim active — `gen 1973`). Identical crash on arm64 + x86_64 = deterministic logic, not a desync. Root cause: the `EFStruct` pool (`EFFECT_ALLOC_NUM=38`) is an unvalidated singly-linked free list (`efManagerSetPrevStructAlloc` pushes the head, `efManagerGetNextStructAlloc` pops + derefs `->next`); the netplay rollback effect-reconcile (`syNetRbSnapEjectGObj`) freed `efGetStruct(gobj)` **without clearing `gobj->user_data.p`** or validating it, so the quake adopt/respawn/stamp machinery ([netplay_quake_effect_verify_drift](netplay_quake_effect_verify_drift_2026-06-07.md) Phases 38–47) — which copies `user_data.p` while reconciling live quakes to slot blobs — could free a stale/aliased struct twice (two GObjs sharing one `EFStruct`); the bad/garbage pointer becomes the free-list head and the next forward quake alloc pops it and SIGSEGVs. The `gcEjectGObj 0xFE` sentinel only guards a second eject of the _same_ GObj, not a duplicate _struct_ free via a different GObj (the aliasing vector); the item-eject sibling already cleared `user_data.p` — the effect path was the lone one that didn't. Fix (decomp allocator untouched, offline never compiles it): capture pool base at init; new netmenu-only `efManagerNetSafeFreeStruct` range-checks `ep` (bounds + element alignment) and walks the free list (bounded) to reject a double-free, **logging the offending eject site** (`SSB64: ef_pool_free_reject reason=... site=... gobj=... id=...`) and skipping the push instead of corrupting the chain; both effect eject sites (`syNetRbSnapEjectGObj` + Yoshi-egg-lay hatch cosmetic) route through it and clear `user_data.p`. Converts a silent corruption + delayed crash into a logged, skipped free that names the producer for the root-cause follow-up. Built clean (`build-netmenu`). Audit hook: a deterministic cross-ISA crash in an effect **allocator** (garbage free-list head) during forward sim after rollback reconcile = a netplay eject pushing a stale/aliased `EFStruct`; every pool-returning rollback eject must clear `user_data.p` and never double-free.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-06-27 | [netplay_grab_throw_release_eff_drift](netplay_grab_throw_release_eff_drift_2026-06-27.md)                                   | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — `netplay-scan-drift.py` soak pair (android host / linux guest) returned `FAIL` on a single `eff`-only `LOAD_HASH_DRIFT` at tick 2857: snapshot captured a non-empty effect fold (`0xBBBC6734`) but the verify load re-hashed to `0x811C9DC5` (FNV empty), with **bit-identical hashes on both peers** (internal save→restore→reload fidelity hole, not a peer desync — same empty-eff symptom family as `netplay_dk_jungle_effect_pop_desync` / `netplay_quake_effect_verify_drift` Phase 38 Class A). The tick is the **grab/throw release boundary**: at 2856 the fighters were still Catch/Throw (`grab_coupling_probe` correctly skipped), but by 2857 the grab had released (status left the range, `catch/capture_gobj_id`=0) so `grab_coupling_probe` no longer fired; the lingering grab spark is present-and-stable across the boundary (so `effect_count_transition_probe` misses it) and coexists with a respawnable shield (so `transient_effect_probe`'s all-transient gate misses it), yet it cannot round-trip the emergency→slot verify load → live fold collapses to empty. Fix: new `syNetRbSnapshotSynctestProbeGrabThrowReleaseBoundaryFragile` (modeled on the `Pupupu Whispy post-blow` "first tick after X ends" precedent) — TRUE when probe slot has effects, no fighter is grab-coupled at the probe tick, but a fighter _was_ grab-coupled at `probe-1`; wired into `ShouldSkipProbeTick` after the grab_coupling block as `grab_coupling_release_boundary_probe`. Because `IsLoadAnchorFragile` reuses `ShouldSkipProbeTick`, real resim also walks its load anchor off this boundary instead of anchoring where the grab VFX can't rebuild. Offline untouched; no live behavior change (peers already agreed). Audit hook: `eff`-only drift with identical peer hashes folding to `0x811C9DC5` one tick after a fighter-state window ends = needs a "first tick after X" boundary probe-skip (cheap) or a per-kind respawn adopter (expensive).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-06-27 | [netrollback_fighter_coupling_gobjid_ambiguity](netrollback_fighter_coupling_gobjid_ambiguity_2026-06-27.md)                 | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — The crash unmasked once the [emergency-restore sparkle-window overflow](netrollback_emergency_restore_sparkle_window_overflow_2026-06-27.md) hang was fixed. The synctest soak then runs to ~tick 2305 and **crashes deterministically** in collision: Linux `SIGBUS fault_addr=0x0`, Android `SIGSEGV fault_addr=0xdc00000000000c` (different bad addrs, same tick = dangling pointer, not a desync; pointers differ per peer even when the sim is bit-identical). Symbolized to `mpProcessCheckTestLWallCollisionAdjNew` (`mpprocess.c:1154`, `p_map_coll->width`); the one-frame unwind is just omit-frame-pointer (`fp=0x42dc...` is a float pattern, benign), the nearby `efManagerMakeEffect` alloc is an incidental hitspark. `p_map_coll` is a transient cross-object source set only by `mpCommonCopyCollDataStats`; the only fighter path supplying a foreign source is the thrown/captured-fighter collision (`ftcommonthrown2.c:76`, `interact_fp = ftGetStruct(this_fp->catch_gobj                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | capture_gobj)`). Root cause = **shared-`gobj_id`ambiguity** (same family as`netrollback_item_gobjid_ambiguity_resim`): all fighters carry `gobj->id == nGCCommonKindFighter (1000)`, the fighter blob persisted the coupling via `syNetRbSnapGobjId`(=1000) and restored it via`syNetRbSnapResolveLiveGobj(1000)`→`gcFindGObjByID(1000)`→ the **first** fighter, never the real partner. After a mid-grab rollback`catch/capture/throw/search_gobj`point at the wrong fighter (often self), the bidirectional rebind reinforces it, and the thrown collision derefs a stale`p_map_coll`. The code already resolves zoom/follow + every other fighter ref by sim player slot (`syNetRbSnapResolveFighterGobjByPlayer`) and items by `syNetRbSnapResolveItemGobj`; the grab/throw coupling was the lone fighter ref still ambiguous. Fix: persist `*\_gobj_player`(sim slot,`-1`=none) for `throw/catch/capture/search` and resolve by player on apply (mirrors the camera). Both netmenu + offline build clean. Audit hook: any rollback restore of a *fighter\* ref must resolve by player slot, never by `gobj->id`; a same-tick crash with per-peer-different bad pointers in a snapshot-restored-GObj deref = the shared-id (fighter 1000 / item 1013) ambiguity class. |
| 2026-06-27 | [netrollback_emergency_restore_sparkle_window_overflow](netrollback_emergency_restore_sparkle_window_overflow_2026-06-27.md) | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — The "`RestoreLiveEmergency` wedging the game thread" follow-up from `netrollback_sector_map_gobj_flags`. With the map-hash fix in, the synctest soak (`SSB64_NETPLAY_ROLLBACK_SYNCTEST=1`) hangs at the intro→GO boundary on **every stage** (Sector Z `gkind=1` _and_ Peach's Castle `gkind=0`): map/`anim`/`item` round-trip, only `cam` drifts (correctly `presentational-only`), then both peers `WATCHDOG HANG` with the **identical** stack refiring every ~2s = true infinite loop. Symbolized against the exact release binary (`build-bundle-linux-netplay-us/BattleShip`, `RestoreLiveEmergency`@`0x5a15b0` matching the AppImage): `syNetRbSnapApplySlotToLive` (:30993) → `syNetRbSnapReconcileLinkBombWindowEffectsCore` (:14738) → `syNetRbSnapshotSlotInLinkBombEffectRepairScope` (:14643 loop) → `syNetRbSnapSlotTickHasExplodeSparkleReplay`. Root cause = **u32 tick overflow**: the scope fn scans a backward sparkle window `for (sparkle_tick = probe_tick-48; sparkle_tick <= probe_tick; sparkle_tick++)` with `probe_tick = slot->tick`; during the emergency restore the slot is the sentinel (`tick == 0xFFFFFFFF`), so `sparkle_tick <= 0xFFFFFFFF` is true for **every** `u32` (after `0xFFFFFFFF`, `++` wraps to 0) → never terminates. Reached whenever the emergency slot has no bomb/fighter-bomb-scope/quake (common at intro), hence stage-independent. Fix: (1) count-bounded scan (`span = probe_tick - sparkle_start <= 48`, iterate an offset so the counter never compares against `UINT32_MAX`); (2) substitute `syNetInputGetTick()` for the `0xFFFFFFFF` sentinel so the window scans live ticks. **Correction:** an earlier pass misdiagnosed this as the unguarded Sector Z Arwing DObj tree walk (frames _inferred_ from raw offsets without symbolizing because the sandbox blocked extracting the AppImage ELF); the Peach's Castle repro — where `syNetRbSnapApplyArwing` early-returns on `gkind != Sector` yet the identical hang persists — disproved it. The misdiagnosed `ApplyArwing`-skip-on-emergency branch was **reverted**; the `GR_SECTOR_ARWING_TREE_WALK_MAX_NODES` (512) tree-walk caps were **kept** as a cheap defensive watchdog (those walks genuinely lacked a cycle guard) but are not the fix. Audit hook: an infinite-loop watchdog hang reached only via the emergency restore = the `0xFFFFFFFF` sentinel tick flowing into tick-window arithmetic; symbolize against the exact build, never infer frames from offsets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-06-27 | [netrollback_sector_map_gobj_flags](netrollback_sector_map_gobj_flags_2026-06-27.md)                                         | **FIX IMPLEMENTED (`PORT && SSB64_NETMENU`, soak pending)** — Sector Z (`stage=1`, gkind=1) cross-ISA soak ends with a fatal, deterministic `LOAD_HASH_DRIFT` at the first tick after the intro (tick 389, the GO boundary): both peers produce **bit-identical** `snap`/`live` hashes (not a peer desync — an internal save→apply→rehash hole), `map`+`cam` diverge, the engine takes `syNetRbSnapshotRestoreLiveEmergency` and the game thread `WATCHDOG HANG`s. The whole intro was unverified (`SYNCTEST_SKIP reason=intro_wait` ×330 while `game_status==Wait`), so the hole only surfaced at the first non-skipped tick. Map diag isolates the drift to the **ground fold** at `first_off=28` = `SYNetRbSnapGroundSector.map_gobj_flags`, the Arwing map GObj visibility flag (`GOBJ_FLAG_HIDDEN`/`NONE`). Dual authority: ground capture stores the **raw** live flag (folded into the map hash) but apply never writes it back — `grSectorSyncArwingMapGObjFlags` **re-derives** it on load from `arwing_status`/`arwing_appear_timer`/root `anim_wait` (all already hashed). Static during intro (round-trips trivially), it diverges the moment those inputs change at GO; `map` is sim-critical and **never soft-continues** → fatal. Fix: exclude `map_gobj_flags` from `syNetRbSnapFoldGroundPayloadHashForRollback` (Sector case, zero it in the fold scratch) like the existing Yoster `map_head` precedent — a derived presentational flag adds no desync coverage (its inputs are hashed) and only breaks the round-trip; tick 389 then reduces to a `cam`-only diff the `presentational-only` path tolerates. Audit hook: a map `LOAD_HASH_DRIFT` with identical peer hashes + `map_hash_ground_payload match=0` = a ground field that is captured raw but reconstructed (not restored) on load. Follow-ups: `intro_wait` synctest skip is a map/cam blind spot; `RestoreLiveEmergency` hanging the game thread is its own bug.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-06-27 | [netplay_aobj_restore_double_unhalfswap_resim](netplay_aobj_restore_double_unhalfswap_resim_2026-06-27.md)                   | ✅ **RESOLVED (`PORT && SSB64_NETMENU`, default-on, env-revertible; soak-confirmed bit-identical Linux↔Android 2026-06-27)** — After a rollback resim, fighter joints rotate continuously / lock into a spin (originally read as "DK's leg," confirmed to affect **all** characters — a deterministic subset of joints on every fighter). No crash; `anim` hash never gates a mismatch. Distinct root cause from [netplay_aobj_interp_quantize_resim](netplay_aobj_interp_quantize_resim_2026-06-26.md) (that fix was necessary but not sufficient); spin persisted with `SIM_F32_QUANTIZE=0` and was **bit-identical Linux↔Android**, ruling out float nondeterminism. Ground truth via `SSB64_NETPLAY_RESIM_STREAM_DIGEST_DIAG`: the forced un-halfswap inside `syNetRbSnapApplyFighterJointPoseAndAnimFromBlob` mutates the figatree EVENT32 bytes **in place at the same cursor** (DK joint 8 `stream=0xb802419c`→`0xde6665ea`, then idempotent — a native→halfswap **double-swap**); soak1 showed **10/38 joints** corrupted across both fighters (P0 8/14/21/25/26, P1 22/23/24/27/32), identical on Android. Root cause: the figatree heap is **shared and not snapshotted**, so at restore its bytes are already native; the blob restores a **mid-stream cursor** the load-time walker never memoized as a head, so the byte-order heuristic mis-applies a swap. The `AOBJ_DOUBLE_SWAP` detector missed it (reads only `sUnswappedHeads`, which lacks mid-stream cursors). Genuine halfswapped bytes only appear after a (re)load, which already evicts the memo via `port_aobj_register_halfswapped_range` + lazy parser re-normalize — so the stable restore path must not re-derive byte order. Fix: gate that un-halfswap behind `SSB64_NETPLAY_RESIM_RESTORE_UNHALFSWAP` (default OFF = fixed); intro-Appear / modelpart / Appear-presentation passes (which follow a genuine reload) keep normalizing. Audit hook: a restore step whose `NetRbStreamDiag` digest changes at the **same `ev=`** is a byte-order writer; trust load-time normalization, not restore-time re-derivation. **Took four parts** (gating the same double-swap at every site that re-derives byte order from a snapshot-restored cursor): (1) the `syNetRbSnapApplyFighterJointPoseAndAnimFromBlob` apply-step force pass; (2) a native-memo re-pin (`port_aobj_event32_head_mark_unswapped`) after `ftMainRefreshFigatreeVisual` evicts the walker memo on reattach (Follow-up 1); (3) the duplicate force pass in `syNetRbSnapApplyFighterModelPartsFromBlob` (Follow-up 2); and — the **decisive** one — (4) the **per-resim-tick repair pass** (`syNetRbSnapshotPreSimUnhalfswapGameplayResimAnim` + four finalize presentation paths) which evicted the memo and force-walked mid-stream native cursors **every replay tick**, collapsing joints to `aw=AOBJ_ANIM_NULL` (P1 j27 fwd `af=30/aw=1` vs resim `af=0/aw=NULL`); routed those five callers through a gated `syNetRbSnapResimRenormalizeFighterFigatreeStreams` that no-ops unless `SSB64_NETPLAY_RESIM_RESTORE_UNHALFSWAP=1`, leaving the genuine intro-Appear fresh-load pass intact (Follow-up 3). Note "drop the `forget()` and trust the memo" can't work standalone: the upstream register/evict wipes the memo before the per-head walk.                                                                                                                                                                                                                        |
| 2026-06-26 | [netplay_aobj_interp_quantize_resim](netplay_aobj_interp_quantize_resim_2026-06-26.md)                                       | ✅ **RESOLVED (`PORT && SSB64_NETMENU`)** — necessary-but-not-sufficient precursor to the resim joint-spin fix, validated as part of the soak-confirmed chain (2026-06-27; the remaining spin was the distinct double-un-halfswap bug, now RESOLVED). A fighter joint (DK's leg) rotates continuously / on a wrong track after a rollback resim instead of resuming pre-resim animation; no crash, rollback reports success. The per-tick `anim` hash matched cross-peer at match start but forked well _before_ the injected mismatch (tick 0/1 identical → tick 518 fully diverged) while `figh`/`world` stayed in sync — and never triggered a rollback because mismatch detection keys on `fhash_light`, not anim (Tier 2B from `netplay_joint_anim_desync_bisect_2026-05-23`). Root cause: the rollback anim fold hashes each joint's AObj interpolation state (`length`/`length_invert`/`value_base`/`value_target`/`rate_base`/`rate_target`) with raw `syNetSyncHashF32`, but the **live sim never quantized those six fields** (only the DObj output pose + anim scalars were), while the **snapshot quantizes all six** on capture/apply — so a snapshot-restored replay starts joints from a different (quantized) interpolation track than the live (unquantized) one, shifting the trajectory at the resim boundary with nothing to reconcile it afterward. Fix: new `syNetplayQuantizeDObjAObjChain` (mirrors `syNetRbSnapCaptureAObjNode`) called in `gcPlayDObjAnimJoint` after `syNetplayQuantizeDObjAnimPose`; corrected stale "quantized again here" comment in `netsync.c`. Audit hook: any AObj/DObj field folded into a rollback hash must be quantized in the live sim where it mutates, not only in snapshot capture/apply.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-06-26 | [android_surface_resize_blast_abort](android_surface_resize_blast_abort_2026-06-26.md)                                       | **FIX IMPLEMENTED (Android build/soak pending)** — Mid-session SIGABRT on the SDL render thread (empty port-handler backtrace); logcat shows `BLASTBufferQueue ... FAILED_TRANSACTION` fatal inside `eglSwapBuffers` (`SDL_EGL_SwapBuffers` → `GfxWindowBackendSDL2::SwapBuffersBegin` → … → `PortPushFrame`), preceded by a window geometry change (`WindowInsets 2231x1080 → 2340x1080`, portrait `943x2231` buffer). Coincided with a netplay match but is a graphics/surface bug, not netplay. Root cause: the app was **not actually landscape-locked at runtime** — SDL overrides the manifest's `screenOrientation=landscape` at window creation (`setOrientationBis`): with no `SDL_HINT_ORIENTATIONS` set + a resizable SDL window (`gfx_sdl2.cpp` uses `SDL_WINDOW_RESIZABLE`) it picks `SCREEN_ORIENTATION_FULL_USER`, so it followed the OS auto-rotate lock and defaulted to portrait; rotating resized the surface and `eglSwapBuffers` applied a stale-size buffer → SurfaceFlinger rejected the transaction → fatal. Fix: set `SDL_SetHint(SDL_HINT_ORIENTATIONS, "LandscapeLeft LandscapeRight")` in `port.cpp portAndroidJniWarmupEarly()` (before LUS window creation) → SDL requests `SENSOR_LANDSCAPE` (landscape-only, both ways). Caveats: doesn't cover multi-window/foldable/inset resizes (consider `resizeableActivity=false`; real hardening = present-path surface-resize guard). Audit hook: BLAST FAILED_TRANSACTION in eglSwapBuffers = surface/swapchain geometry mismatch; verify SDL is orientation-locked, not just the manifest.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-06-26 | [android_matchmaking_cares_dns_fdsan](android_matchmaking_cares_dns_fdsan_2026-06-26.md)                                     | **FIX IMPLEMENTED (Android build/soak pending)** — Android client SIGABRT immediately on entering matchmaking (before any ICE agent), empty backtrace; logcat `fdsan: attempted to close file descriptor 129 ... actually owned by Parcel` on the matchmaking worker thread. Root cause: Android curl was built `ENABLE_ARES=OFF`/`ENABLE_THREADED_RESOLVER=ON`, so name resolution used Android `getaddrinfo` → `netd`/`DnsResolver` **Binder**, whose Parcel-backed fds were double-closed under bionic fdsan during the worker's back-to-back heartbeat → `GET /v1/turn-credentials` requests (fresh handle + new DNS each time maximizes fd reuse). mbedTLS/raw sockets never touch Binder, so the Parcel fd is definitively the system resolver. Same "owned by Parcel" variant the 2026-05-30 doc only rate-limit-mitigated. Fix: switch Android curl to **c-ares** async DNS (`Ssb64CurlAndroid.cmake` FetchContent c-ares 1.34.5 static+PIC; new `FindCares.cmake` shim mirroring `FindMbedTLS`; `patch_curl_android.cmake` `PATCH_FIND_CARES`). c-ares can't auto-discover Android DNS servers (net.dns\* gone in 8.0) so `mm_matchmaking.c` sets `CURLOPT_DNS_SERVERS` (default `1.1.1.1,8.8.8.8,9.9.9.9`, env `SSB64_MATCHMAKING_DNS_SERVERS`); future hardening = `ares_library_init_android`. Desktop unaffected (vcpkg curl; cmake is Android-only). Audit hook: fdsan "owned by Parcel" on a curl thread → system DNS resolver, not libjuice.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-06-26 | [netrollback_item_gobjid_ambiguity_resim](netrollback_item_gobjid_ambiguity_resim_2026-06-26.md)                             | **FIXED (`PORT && SSB64_NETMENU`)** — Deep rollbacks with two-plus identical live items (e.g. two Link bombs on Peach's Castle) desynced on reload: `LOAD_HASH_DRIFT` on the item subsystem → resim retry storm → `SIGABRT` in `gcRunGObjProcess` when a mis-paired item's `owner_gobj` resolved NULL (orphan-hold → `itMainDestroyItem` deref). Root cause: all items share the `nGCCommonKindItem` `gobj_id` (1013), and `syNetRbSnapFindItemBlobForLiveGobj`'s `gobj_id`+kind fast-path returned the _first-in-slot_ blob — not a stable identity once a walk-back respawns/reorders items, so one bomb absorbed the other's blob (pos/vel/owner swapped). `SYNetRbSnapItemBlob` has no per-instance `instance_id` to tie-break. Fix: gate the fast-path on uniqueness (`syNetRbSnapCountUnmatchedItemBlobsByGobjIdKind <= 1`); ambiguous multi-instance cases fall through to the existing position/velocity matchers (`syNetRbSnapFindLinkBombBlob` / `...ByKindPos`). Single-instance unchanged. Repro: `debug-resimtest.env` FORCE_MISMATCH @ tick 520. Audit hook: item resim drift that only appears with multiple identical items live → shared `gobj_id` mispairing. Long-term: add per-instance id to the item blob.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-06-16 | [overscan_black_border](overscan_black_border_2026-06-16.md)                                                                 | **FIXED (libultraship + port menu)** — A black border framed the game image on all four sides in every window size and fullscreen (distinct from the 4:3 pillarbox; predates the libultraship integration). Root cause: SSB64 bakes a 10px "title-safe" overscan margin into its own GBI — the default scissor (`sys/rdp.c` `syRdpResetSettings` → `(10,10)..(310,230)`) and every per-camera scissor (`sys/objdisplay.c` `dGCCameraScissor{...}=10`) clip all drawing _and the per-frame framebuffer clear_ to the safe area, so the 10px margin is never drawn into or even cleared. A CRT's overscan hid it; presenting the full 320×240 framebuffer 1:1 to a desktop window exposes it. Can't just widen the scissor (would reveal uncleared garbage in the margin). Fix: present-level overscan crop in `Gui::DrawGame` — the `ImGui::Image` blit samples the framebuffer minus its proportional margin (`10/320` x, `10/240` y), stretching the safe area to fill the viewport, reproducing the on-TV image. Resolution-independent, orientation-symmetric, skipped in pixel-perfect mode. CVar-gated (`gAdvancedResolution.Overscan.{Enabled,Horizontal,Vertical}`, default on/10/10) with menu toggle + sliders under Enhancements; takes effect immediately (no reload). Audit hook: "image inset from window" → distinguish 4:3 pillarbox (left/right only) vs this overscan frame (all four sides) vs an ImGui dockspace sizing bug (would also affect menu bar / floating windows).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-06-15 | [funchook_macos_arm64_patch_anchor](funchook_macos_arm64_patch_anchor_2026-06-15.md)                                         | **FIXED (build)** — TCC mod funchook inline detours silently failed on Apple Silicon (`mprotect(RWX)` → EINVAL on code-signed `__TEXT`) even with JIT entitlements + maxprot re-sign. The FetchContent patch that rewrites funchook to `mach_vm_protect`/`VM_PROT_COPY` never reached the call sites: its CMake anchors used `\;`, which renders as a literal backslash-semicolon, so `string(REPLACE)` no-op'd and only the (now-dead) helper defs got inserted. Fixed anchors to plain `;` + hardened the idempotency guard to key on the wrapped call site (not the helper name). Scripting/events/symbol-resolution were unaffected; only raw detours. Wipe `build*/_deps/funchook-*` to re-patch. Verified via `workspace/hooktest` (1263 detour frames).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-06-10 | [controller_port_routing](controller_port_routing_2026-06-10.md)                                                             | **FIXED (libultraship)** — Second gamepad (any type) effectively unusable for local multiplayer: three independent defects. (1) Input-editor device rows derived ImGui IDs from device names → two same-model pads collided (warning + hover cross-talk); fixed with instance-id ID suffix + `#1`/`#2` display ordinals. (2) `RefreshConnectedSDLGamepads` ran on every device add/remove event and re-ignored every pad for ports 2-4, silently reverting user routing; now only never-seen instance ids get defaulted. (3) All pads defaulted to port 1 AND only port 1 got default mappings, so a pad routed to port 2 was dead without a manual "Set Defaults"; now Nth pad auto-assigns to first free port and ports 2-4 seed SDL defaults at init. Diagnostic gotchas: same-model pads share SDL GUIDs (per-model, not per-unit); serials are null under macOS GCController driver; headless SDL probes see zero pads without a pumped runloop                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-06-10 | [crash_handler_devnull_probe](crash_handler_devnull_probe_2026-06-10.md)                                                     | **FIXED — corrects a 2026-06-09 audit item** — Crash handler double-faulted on any game-coroutine crash: `ProbeReadable` wrote probed bytes to /dev/null, whose driver (XNU `nullwrite` / Linux `write_null`) never copies from the source buffer, so the probe reported EVERY address readable. `WalkFPChain` then stepped onto the adjacent coroutine stack's `PROT_NONE` guard page at the chain's base frame → nested SIGBUS inside the handler, original backtrace lost, macOS report blamed `port_watchdog.cpp`. Fingerprint: two `!!!! CRASH` lines in ssb64.log, second always SIGBUS at a page-aligned addr. Fix: probe via self-pipe (pipe writes copyin → genuine EFAULT), fds created at watchdog init, drained after each probe. Audit hook: a `write()`-based readability probe must target a pipe/socket, never /dev/null                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
